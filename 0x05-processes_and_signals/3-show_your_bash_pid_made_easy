#!/usr/bin/env bash
# This script displays the PID and process name of processes containing the word "bash."

# Use pgrep to find processes containing "bash" and display their PID and name
pgrep -a bash
